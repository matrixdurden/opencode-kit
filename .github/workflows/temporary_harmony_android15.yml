name: Temporary Harmony Android 15 APK build

on:
  pull_request:
    branches:
      - main

jobs:
  build:
    name: Build Harmony Music Android APK
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Harmony Music Android 15 fix branch
        uses: actions/checkout@v4
        with:
          repository: matrixdurden/harmony-music
          ref: fix/android15-install

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          flutter-version: '3.24.2'
          cache: true

      - name: Setup JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Install packages
        run: flutter pub get

      - name: Build release APK
        run: flutter build apk --release

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: harmony-music-android15-install-fix
          path: build/app/outputs/flutter-apk/app-release.apk
          if-no-files-found: error
